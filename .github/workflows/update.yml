name: 🚀 Update
on:
  push:
    branches:
      - development
  pull_request: {}

jobs:
    update:
        runs-on: ubuntu-latest
        steps:
            - name: 🛠 Setup node
              uses: actions/setup-node@v4
              with:
                node-version: 20

            - name: 📦 Install dependencies
              run: npm install

            - name: 🚀 Update
              run: npm run tst





